{
  "name": "refinement-ts",
  "version": "0.0.0",
  "main": "dist/index.js",
  "author": "Wayne Van Son",
  "repository": "https://github.com/waynevanson/refinement-ts",
  "license": "MIT",
  "scripts": {
    "start": "ts-node src/index.ts",
    "watch": "jest --watchAll",
    "test": "jest",
    "build": "tsc --project ./tsconfig.build.json",
    "prepublishOnly": "yarn test && yarn build"
  },
  "devDependencies": {
    "@types/jest": "^26.0.4",
    "jest": "^26.1.0",
    "ts-jest": "^26.1.1",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.6"
  }
}
